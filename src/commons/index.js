import { Alert, Footer, Header, InfoErrors, Loading } from './components';
import messages from './messages/messages';
import labels from './messages/labels';

export {
    Alert, 
    Footer, 
    Header, 
    InfoErrors, 
    Loading,
    messages,
    labels
}